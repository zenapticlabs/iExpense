name: Deploy Expo Web to Vercel

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Debug Secrets
      run: |
        echo "Checking secrets..."
        if [[ -z "${{ secrets.VERCEL_PROJECT_ID }}" ]]; then echo "❌ VERCEL_PROJECT_ID is MISSING"; else echo "✅ VERCEL_PROJECT_ID is SET"; fi
        if [[ -z "${{ secrets.VERCEL_ORG_ID }}" ]]; then echo "❌ VERCEL_ORG_ID is MISSING"; else echo "✅ VERCEL_ORG_ID is SET"; fi
        if [[ -z "${{ secrets.VERCEL_TOKEN }}" ]]; then echo "❌ VERCEL_TOKEN is MISSING"; else echo "✅ VERCEL_TOKEN is SET"; fi
        if [[ -z "${{ secrets.EXPO_PUBLIC_API_URL }}" ]]; then echo "❌ EXPO_PUBLIC_API_URL is MISSING"; else echo "✅ VERCEL_TOKEN is SET"; fi

      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install lightningcss manually
        run: npm install lightningcss --legacy-peer-deps

      - name: Install Dependencies
        run: npm install
      
      - name: Create .env File
        run: |
          echo "EXPO_PUBLIC_API_URL=${{ secrets.EXPO_PUBLIC_API_URL }}" > .env

      - name: Build Expo Web
        run: npm run build:web

      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          vercel-args: "--prod"
          working-directory: dist

